services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: neobank
    ports:
      - "5432:5432"
    volumes:
      - neobank_pg:/var/lib/postgresql/data

volumes:
  neobank_pg:
